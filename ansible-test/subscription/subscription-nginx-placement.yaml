apiVersion: apps.open-cluster-management.io/v1
kind: Subscription
metadata:
  name: placement-nginx-subscription
  namespace: nginx-pre-test
  labels:
    app: nginx-placement
  annotations:
    apps.open-cluster-management.io/github-path: ansible-test/nginx
    apps.open-cluster-management.io/github-branch: master
spec:
  channel: demo/gitops
  hooksecretref:
    name: toweraccess
  placement:
    placementRef:
      name: nginx-placement
      kind: PlacementRule

