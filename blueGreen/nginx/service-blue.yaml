apiVersion: v1
kind: Service
metadata:
  name: blue-nginx-service
spec:
  selector:
    app: nginx
    stage: blue 
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 80
