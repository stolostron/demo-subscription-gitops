apiVersion: apps.open-cluster-management.io/v1
kind: PlacementRule
metadata:
  name: nginx-deployment
  namespace: nginx
  generation: 1
  labels:
    app: nginx-deployment
spec:
  clusterLabels:
  matchLabels:
    cloud: Amazon